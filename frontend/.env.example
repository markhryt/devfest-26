# Demo mode: no Flowglad billing calls, no /api/flowglad 404s. Set to true for local testing.
NEXT_PUBLIC_DEMO_MODE=true

# Backend URL (backend runs on 4000 by default)
NEXT_PUBLIC_API_URL=http://localhost:4000
